---
concept: example
deprecated_terms: [template_id, instance_key, substrate]
---

# Example

The body uses only the current vocabulary: template_hash and instance_key. The frontmatter
above declares the deprecated synonyms in `deprecated_terms:` — that is the canonical
declaration site, and the lint must not scan inside the frontmatter block.
