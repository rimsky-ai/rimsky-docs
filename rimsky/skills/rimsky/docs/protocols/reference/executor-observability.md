<!-- AUTOGENERATED by rimsky-docs-proto from rimsky lib/protocols/proto/v1/. Do not hand-edit. -->

# Executor Observability

Source: `lib/protocols/proto/v1/executor_observability.proto`

## Services

### ExecutorObservability

ExecutorObservability is the optional read-only protocol every
executor MAY implement to expose per-dispatch traces to dashboards.
The dispatch protocol (executor.proto) is unchanged. See
docs/specs/2026-05-02-dashboard-and-observability-design.md §2.

#### ExecutorObservability.Capabilities

- Request: `ExecutorCapabilitiesRequest`
- Response: `ObservabilityCapabilities`

#### ExecutorObservability.GetTrace

- Request: `GetTraceRequest`
- Response: `Trace`

#### ExecutorObservability.StreamTrace

- Request: `StreamTraceRequest`
- Response: `stream TraceEvent`

## Messages

### ExecutorCapabilitiesRequest

### ObservabilityCapabilities

| Field | Type | # | Description |
|-------|------|---|-------------|
| `supports_trace_get` | `bool` | 1 |  |
| `supports_trace_stream` | `bool` | 2 |  |
| `retention_after_terminal_seconds` | `uint64` | 3 |  |
| `custom_ui` | `CustomUI` | 4 |  |
| `http_bridge_url` | `string` | 5 | http_bridge_url, when non-empty, is the absolute base URL the service serves the HTTP+JSON observability bridge on. Dashboard clients dial this URL directly for browser-friendly fetch/SSE access. When empty, services expose only the gRPC surface. |
| `expected_attributes_schema` | `bytes` | 6 | expected_attributes_schema is a JSON Schema (RFC 8259 + draft 2020-12) describing the executor's accepted attribute shape. Empty means "no schema; accept any attributes." Each property in the schema is one of: - an *input* the executor consumes at dispatch (no `readOnly`); or - an *output* the executor produces at commit write-back, marked `readOnly: true` per the JSON Schema standard keyword. Rimsky merges this schema with template-level defaults (L1) and per-node declarations (L2) at registration to compute the effective attribute schema, then validates the post-substitution attribute bag at dispatch and the post-write-back bag at commit. Validation failures route through Error { error_class: "template_validation_failed" }. |
| `declared_events` | `repeated string` | 7 | declared_events is the set of event names this executor may emit via the NamedEvent wire type on ExecuteEvent. Empty means "executor does not emit events." Rimsky validates that template subscriptions of the form `subscribes: [{node: <sender>, type: event/<name>}]` reference an event in declared_events. |
| `declared_error_classes` | `repeated string` | 8 | declared_error_classes is the set of error-class paths this executor may emit on Error.error_class. Patterns ending in `*` indicate prefix-pattern leaves (e.g., `http/server_error/*`); exact strings indicate fixed leaves. The validator's range-check of operator `error_types:` keys accepts a key if it exactly matches a declared plain leaf OR matches a declared `<prefix>/*` pattern by prefix. Empty/absent means "executor does not declare; skip validator range-check for this executor". Per concept:signal hierarchical error_class rule. |

### CustomUI

| Field | Type | # | Description |
|-------|------|---|-------------|
| `ui_url` | `string` | 1 |  |
| `embed_mode` | `EmbedMode` | 2 |  |
| `dispatch_url_template` | `string` | 3 |  |

### GetTraceRequest

| Field | Type | # | Description |
|-------|------|---|-------------|
| `dispatch_id` | `string` | 1 |  |

### StreamTraceRequest

| Field | Type | # | Description |
|-------|------|---|-------------|
| `dispatch_id` | `string` | 1 |  |

### Trace

| Field | Type | # | Description |
|-------|------|---|-------------|
| `dispatch_id` | `string` | 1 |  |
| `evicted` | `bool` | 2 |  |
| `complete` | `bool` | 3 |  |
| `events` | `repeated TraceEvent` | 4 |  |

### TraceEvent

| Field | Type | # | Description |
|-------|------|---|-------------|
| `event_id` | `string` | 1 |  |
| `parent_event_id` | `string` | 2 |  |
| `timestamp` | `Timestamp` | 3 |  |
| `severity` | `Severity` | 4 |  |
| `category` | `string` | 5 |  |
| `message` | `string` | 6 |  |
| `attributes` | `Struct` | 7 |  |

## Enums

### EmbedMode

| Value | # | Description |
|-------|---|-------------|
| `EMBED_MODE_UNSPECIFIED` | 0 |  |
| `LINK` | 1 |  |
| `IFRAME` | 2 |  |
| `BOTH` | 3 |  |

### Severity

| Value | # | Description |
|-------|---|-------------|
| `SEVERITY_UNSPECIFIED` | 0 |  |
| `DEBUG` | 1 |  |
| `INFO` | 2 |  |
| `WARN` | 3 |  |
| `ERROR` | 4 |  |

