# Per-store-service config for the `store-postgres` reference service.
# Mounted at /etc/store/config.yml inside the container.
connection: "postgres://rimsky:rimsky@postgres:5432/rimsky?sslmode=disable"
# write_semantics declares the single value this store-service realizes
# on every Open. Operators may declare a different (non-empty) subset
# of this in rimsky.yml's claim_producers[*].write_semantics_allowed.
write_semantics: sync
pick_policies:
  "@review-queue":
    items_table: topics_items
    on_commit: recycle
    on_give_up: recycle
    visibility_timeout_seconds: 300
host: 0.0.0.0
grpc_port: 9101
http_port: 9111
http_bridge_url: "http://store-postgres:9111"
admin_port: 9121
sweep_interval_seconds: 30
