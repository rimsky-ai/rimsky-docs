# Stub claim-producer config for the quickstart. In-memory state;
# deterministic behavior. Mounts into the store-stub container at
# /etc/store/config.yml via STORE_STUB_CONFIG.

write_semantics_allowed: [sync]
pick_policies: {}

host: 0.0.0.0
grpc_port: 9102
http_port: 9112
