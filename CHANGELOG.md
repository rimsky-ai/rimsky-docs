# Changelog

## Unreleased

- **Post-bootstrap fixes for the lint binaries' concepts-dir default.** Caught by code review. `cmd:rimsky-docs-{glossary,llms-full,lint}`'s `concepts-dir` flag defaulted to `../docs/concepts` — but this repo's `docs/` has no `concepts/` subdirectory (the concept catalog lives in rimsky at `.ok-planner/design/concepts/`). The pre-release reconciliation gate was therefore structurally broken: `glossary-parity` would compare an empty generator output against the checked-in `docs/glossary.md` and always fail; `citation-drift` would scan with an empty target map and silently report zero hits. Defaults now compose from `env:RIMSKY_REPO` (which all three binaries already require to be set) → `${RIMSKY_REPO}/.ok-planner/design/concepts`. Also: `pkg:examples/atomic-staging-fs-producer/go.mod` dropped its dead `replace github.com/fallguyconsulting/rimsky/sdk/go` directive — no require entry, no import site, so the directive was a no-op that misled future readers.

- **Repo bootstrapped from rimsky's `file:docs/`, `file:cmd/rimsky-docs-*`, and `file:examples/atomic-staging-fs-producer/` trees per P4 of `spec:2026-05-24-repo-reorganization-design`.** Pass 7 of `plan:2026-05-24-repo-reorganization`. Contents on day one:
  - `file:docs/` — 49 markdown files migrated from rimsky's `docs/` tree (concepts, protocols, agents guide, blob-backend / store / executor / mcp-server references, patterns, operator guide).
  - `file:cmd/` — three lint binaries (`cmd:rimsky-docs-lint`, `cmd:rimsky-docs-llms-full`, `cmd:rimsky-docs-glossary`) under one Go module (`pkg:github.com/fallguyconsulting/rimsky-docs/cmd`) with stdlib + `pkg:gopkg.in/yaml.v3` dependencies only. Defaults retargeted at the new rimsky-docs layout (`../docs/concepts`, `../docs/agents/...`, `../.vocabulary-lint.yml`, `../llms.txt`, `../llms-full.txt`) so binaries are invoked from `cmd/` cwd. `code:rimsky-docs-lint/glossary_parity.go` shells out to `./rimsky-docs-glossary` (no longer the rimsky-side `./cmd/rimsky-docs-glossary` path). `code:rimsky-docs-llms-full/main.go::run` now writes both `docs/agents/llms-full.txt` and the repo-root `llms-full.txt` copy in a single invocation via the new `-root-output` flag (subsumes the rimsky-side `Makefile docs-roots` target's `cp` step).
  - `file:examples/atomic-staging-fs-producer/` — compilable Go reference impl (cmd / server / store / sweep / template.yaml / README.md) under its own Go module (`pkg:github.com/fallguyconsulting/rimsky-docs/examples`). Imports `pkg:github.com/fallguyconsulting/rimsky/protocols/proto/v1/gen` cross-repo via a local `replace` directive (matches the pattern established in `pkg:github.com/fallguyconsulting/rimsky-services` Pass 5).
  - `file:examples/atomic-staging-fs-producer/scenarios/` — four scenario tests (`abandon_on_any_failure_test.go`, `commit_on_all_success_test.go`, `concurrent_staging_test.go`, `sub_stage_verifier_failure_test.go`) migrated alongside their example. All four pass without modification beyond import-path retargeting; none required `t.Skip` (they exercise the example's own `store` package, not `pkg:graph/scenario`).
  - `file:.vocabulary-lint.yml` at repo root (moved from `file:docs/.vocabulary-lint.yml` so the file lives next to the binary that reads it; rimsky-docs-lint's `vocabulary` subcommand default points here).
  - `file:llms.txt` and `file:llms-full.txt` at repo root — verbatim copies of `file:docs/agents/llms.txt` and `file:docs/agents/llms-full.txt` (regenerated by `cmd:rimsky-docs-llms-full -root-output=...`). Migrated from rimsky's repo root.
- **`env:RIMSKY_REPO` convention documented in `file:README.md`.** Each of the three lint binaries (`cmd:rimsky-docs-{lint,llms-full,glossary}`) requires `env:RIMSKY_REPO` to point at a local rimsky checkout and exits 2 with a helpful message when unset. `cmd:rimsky-docs-lint`'s `public-anchor-validity` subcommand anchors its `-proto-dir` default at `${RIMSKY_REPO}/protocols/proto/v1` (the proto sources live in rimsky, not rimsky-docs). Other subcommands operate on rimsky-docs's own `docs/` tree but the `env:RIMSKY_REPO` requirement is enforced uniformly so the pre-release reconciliation gate in rimsky's `file:scripts/release.sh` can rely on a single contract.
